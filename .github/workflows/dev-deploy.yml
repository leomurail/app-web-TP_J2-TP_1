name: Dev Deployment

on:
  push:
    branches: [ develop ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create destination directory
        run: mkdir -p /home/ubuntu/dev

      - name: Sync files
        run: |
          rsync -avz --delete \
            --exclude '.git' \
            --exclude '.github' \
            ./ /home/ubuntu/dev/

      - name: Initialize Environment
        run: |
          cd /home/ubuntu/dev
          task install
        env:
          APP_ENV: dev
