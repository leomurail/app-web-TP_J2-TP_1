version: '3.8'

services:
  app:
    ports:
      - "3060:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Useful for Docker Watch/hot reload
